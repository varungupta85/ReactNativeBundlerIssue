const ContextMenu = (function() {
  const show = (ctxOptions) => {
    const {items, handlers, info} = ctxOptions
    const cancelButtonIndex = info.cancelButtonIndex || -1
  }

  return {
    show
  }
})()

export default ContextMenu
